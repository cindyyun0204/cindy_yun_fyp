# FYPServer

Server start: create an virtual env, then install httpx and uvicorn.
command: uvicorn server:app --host 127.0.0.1 --port 8000

Ollama: download qwen2.5-vl:latest
and use ollama pull qwen2.5-vl:latest
